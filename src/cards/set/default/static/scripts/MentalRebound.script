import com.wcecil.actions.core.DiscardCard
import com.wcecil.core.GameController;

def discard = new DiscardCard(targetPlayer:game.currentPlayer, cause:null, sourcePlayer:source);

GameController.doAction(game, discard)